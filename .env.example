# AmarktAI Network
# Copy to .env and replace placeholders. Never commit secrets.

# ── Core ─────────────────────────────────────────────────────────────────────
DATABASE_URL="mysql://amarktai:STRONG_PASSWORD@127.0.0.1:3306/amarktai"
SESSION_SECRET="REPLACE_WITH_64_HEX_CHARACTERS"
APP_URL="http://localhost:3000"
NEXT_PUBLIC_APP_URL="http://localhost:3000"
# Set to "true" in production (HTTPS required for secure cookies)
COOKIE_SECURE="true"
# AES-256-GCM key for API key encryption at rest (32 random bytes, hex-encoded)
VAULT_ENCRYPTION_KEY="REPLACE_WITH_64_HEX_CHARACTERS"

# ── Approved AI Providers ─────────────────────────────────────────────────────
# GenX — primary gateway (routes to GPT-4o, Claude, Gemini, DeepSeek etc.)
GENX_API_KEY=""
GENX_BASE_URL="https://query.genx.sh"

# Hugging Face — open-source models, embeddings, TTS, STT
HUGGINGFACE_API_KEY=""
# Optional aliases: HUGGINGFACEHUB_API_TOKEN or HF_TOKEN
# Per-capability overrides: HF_MODEL_<CAPABILITY> and HF_ENDPOINT_<CAPABILITY>
HF_SPECIALIST_MODELS_JSON="{}"
HF_SPECIALIST_ENDPOINTS_JSON="{}"
HF_ENDPOINT_RERANK=""
HF_ENDPOINT_IMAGE_EDIT=""
HF_ENDPOINT_TEXT_TO_VIDEO=""
HF_ENDPOINT_IMAGE_TO_VIDEO=""
HF_ENDPOINT_MUSIC_GENERATION=""
HF_ENDPOINT_TTS=""
HF_ENDPOINT_STT=""

# Qwen / DashScope / WanX — Alibaba Cloud: chat, vision, image/video generation
QWEN_API_KEY=""
# DASHSCOPE_API_KEY is also accepted.
# Keep Qwen free-token-only by default. Set to "true" only if paid routing is
# intentionally enabled.
QWEN_PAID_ENABLED="false"
DASHSCOPE_BASE_URL="https://dashscope-intl.aliyuncs.com/compatible-mode/v1"
DASHSCOPE_AIGC_BASE_URL="https://dashscope-intl.aliyuncs.com/api/v1"

# Xiaomi MiMo — reasoning model
MIMO_API_KEY=""
XIAOMI_API_KEY=""
MIMO_BASE_URL="https://api.xiaomimimo.com/v1"
MIMO_RUNTIME_API_ENABLED="false"

# Groq — ultra-low-latency inference (Llama, Mixtral, Whisper)
GROQ_API_KEY=""

# Together AI — open-source model hosting (Llama, FLUX, etc.)
TOGETHER_API_KEY=""
TOGETHER_BASE_URL="https://api.together.ai/v1"
TOGETHER_VIDEO_BASE_URL="https://api.together.ai/v1"
TOGETHER_VIDEO_RUNTIME_ENABLED="false"
TOGETHER_DEDICATED_ENDPOINTS_JSON="{}"

# ── Infrastructure ────────────────────────────────────────────────────────────
GITHUB_PAT=""
REDIS_URL="redis://127.0.0.1:6379"
QDRANT_URL="http://127.0.0.1:6333"
QDRANT_API_KEY=""
AMARKTAI_PYTHON_BIN="/var/www/amarktai/.venv/bin/python"
FFMPEG_PATH=""
FFPROBE_PATH=""
RHUBARB_PATH=""
LIPSYNC_SERVICE_URL=""

# ── Storage ───────────────────────────────────────────────────────────────────
STORAGE_DRIVER="local_vps"
AMARKTAI_STORAGE_ROOT="/var/www/amarktai/storage"
AMARKTAI_ROUTING_POLICY="auto"
VPS_MONITOR_API_KEY=""
VPS_WARNING_CPU_PERCENT="85"
VPS_WARNING_RAM_PERCENT="85"
VPS_WARNING_DISK_PERCENT="80"
# Set to "false" in production — dev fallback must be explicitly enabled
AMARKTAI_ALLOW_DEV_STORAGE_FALLBACK="false"

# ── Notifications ─────────────────────────────────────────────────────────────
SMTP_HOST=""
SMTP_PORT="587"
SMTP_USER=""
SMTP_PASS=""
SMTP_FROM="Amarktai Network <noreply@amarktai.co.za>"
CONTACT_EMAIL=""
ALERT_EMAIL=""

# ── Admin Bootstrap ───────────────────────────────────────────────────────────
# ADMIN_PASSWORD is a bootstrap-only fallback. Remove it after creating a DB
# admin user via the onboarding wizard.
WEBDOCK_API_KEY=""
ADMIN_EMAIL="admin@amarktai.co.za"
ADMIN_PASSWORD="REPLACE_WITH_SECURE_PASSWORD"

# ── Realtime ──────────────────────────────────────────────────────────────────
REALTIME_SERVICE_URL="http://localhost:3001"
