# Active Open Source Stack

Last audited: 2026-06-18

This file is the source-of-truth checklist for local/open-source runtime pieces used by V1 proof, Studio generation, research, media assembly, worker polling, and artifact persistence. It is evidence inventory, not a readiness claim: VPS proof and Settings checks still have to pass in the deployed environment.

| Component | Installed | Wired | Service running | Used by capability | Exact setup command | Exact blocker | Safe next step |
|---|---|---|---|---|---|---|---|
| Redis | Yes: `ioredis` dependency. Local `redis-server` binary/process not found in this Codex run. | Yes: `src/lib/job-queue.ts`, `src/lib/control-plane-jobs.ts`, Redis settings test. | Local: no. VPS: must be checked with `systemctl status redis-server` or `/api/admin/settings/test-redis`. | BullMQ queues, async media/control-plane jobs, worker retry/polling proof. | `sudo apt-get install -y redis-server && sudo systemctl enable --now redis-server`; set `REDIS_URL=redis://127.0.0.1:6379`. | Blocked if `REDIS_URL` is absent or Redis is unreachable from both app and worker. | Run the Settings Redis test and proof harness on VPS. |
| BullMQ worker | Yes: `bullmq` dependency and `scripts/worker.mjs`. | Yes: `src/lib/worker.ts`, `src/lib/job-queue.ts`, `scripts/worker.mjs`. | Local: not running during audit. VPS: must be checked with systemd/process list. | Async provider jobs, control-plane attempts, retries. | Foreground: `npm run worker`; production: create/enable `amarktai-worker.service` running `npm run worker` in the deployed repo. | Blocked if no worker process is running the same commit and `.env` as the Next app. | Add/verify systemd unit, then run worker/job retry proof. |
| Qdrant | Yes: `@qdrant/js-client-rest` dependency. | Yes: vector/retrieval integration and settings surfaces. | Local: not verified running. VPS: check `QDRANT_URL` and health endpoint. | Retrieval, semantic memory, research enrichment. | `docker run -d --name qdrant -p 6333:6333 -v qdrant_storage:/qdrant/storage qdrant/qdrant`; set `QDRANT_URL=http://127.0.0.1:6333`. | Blocked if `QDRANT_URL` is missing or collection service is unreachable. | Run retrieval/Qdrant Settings check on VPS. |
| Playwright | Yes: npm package resolves locally. | Yes: `src/lib/local-tools.ts`, `src/lib/research-tools.ts`, research URL tooling. | Local package: yes. Browser install: not proven in this pass. | Web research rendering/crawling. | `npx playwright install --with-deps chromium`. | Blocked if Chromium browser dependencies are absent for the app/worker user. | Run browser install check on VPS and web research smoke. |
| Scrapy | No local Python module in this Codex run. | Partially: `src/lib/local-tools.ts`, `src/lib/research-tools.ts`, install script. | Local: no. VPS: unknown until Python module check. | Web research crawl fallback. | `python -m pip install scrapy` or run `bash scripts/install-open-source-stack.sh`. | Blocked for Scrapy-backed crawl proof until installed in the Python used by `PYTHON_PATH` or system `python`. | Install into the VPS runtime Python and rerun research proof. |
| Trafilatura | No local Python module in this Codex run. | Partially: `src/lib/local-tools.ts`, `src/lib/research-tools.ts`, install script. | Local: no. VPS: unknown until Python module check. | Web research extraction. | `python -m pip install trafilatura` or run `bash scripts/install-open-source-stack.sh`. | Blocked for high-quality extraction proof until installed in the Python used by `PYTHON_PATH` or system `python`. | Install into the VPS runtime Python and rerun research proof. |
| ffmpeg | External binary expected; not on local PATH in this Codex run. | Yes: `src/lib/local-tools.ts`, `src/lib/long-form-video.ts`, media assembly paths. | Local: no. VPS: check `ffmpeg -version` as app user. | Long-form video, audio/video post-processing, avatar/video assembly. | `sudo apt-get install -y ffmpeg`; optional `FFMPEG_PATH=/usr/bin/ffmpeg`. | Blocked if `ffmpeg -version` fails for the app/worker user. | Install/verify ffmpeg on VPS before long-form video proof. |
| ffprobe | External binary expected; not on local PATH in this Codex run. | Yes: `src/lib/local-tools.ts`, `src/lib/long-form-video.ts`. | Local: no. VPS: check `ffprobe -version` as app user. | Video metadata/proof validation. | Installed with ffmpeg: `sudo apt-get install -y ffmpeg`; optional `FFPROBE_PATH=/usr/bin/ffprobe`. | Blocked if `ffprobe -version` fails for the app/worker user. | Install/verify ffprobe on VPS before video validation proof. |
| Rhubarb/lip-sync | No binary bundled; not on local PATH in this Codex run. | Tool check exists; canonical avatar-video adapter boundary is not fully proven. | Local: no. VPS: unknown. | Talking avatar video/lip-sync. | Install Rhubarb and set `RHUBARB_PATH=/absolute/path/to/rhubarb`; then wire/verify approved adapter/service boundary. | `talking_avatar_video` remains `BLOCKED` until an approved Rhubarb/lip-sync binary/service adapter is configured and proven. | Decide Rhubarb binary vs service boundary, install, then run avatar-video proof. |
| Local artifact storage | Yes: built-in `local_vps` storage driver. | Yes: `src/lib/storage-driver.ts`, `src/lib/artifact-store.ts`, `/api/artifacts/file/[...key]`, artifact download route. | Local code path present. VPS: writable root must be checked. | Image/video/audio/music/avatar previews and downloads, durable text/report artifacts. | `sudo mkdir -p /var/www/amarktai/storage/{artifacts,uploads,repos,workspaces,logs} && sudo chown -R <app-user>:<app-user> /var/www/amarktai/storage`; set `STORAGE_DRIVER=local_vps` and `AMARKTAI_STORAGE_ROOT=/var/www/amarktai/storage`. | Blocked if storage root is not writable/readable/deletable by the app and worker. | Run `/api/admin/settings/test-storage` and a media artifact smoke on VPS. |
| Worker service | Script exists. | Yes: worker entrypoint and queue code exist. | Local: not running during audit. VPS: must be checked with systemd/process list. | Async generation completion, retry, polling, control-plane proof. | Create systemd unit running `npm run worker`, then `sudo systemctl enable --now amarktai-worker`. | Blocked if the worker is stopped, running old code, or missing the same `.env` as the web process. | Verify unit commit/env parity, then rerun proof. |

Current local audit notes:

- Node packages resolve locally for `playwright`, `bullmq`, `ioredis`, and `@qdrant/js-client-rest`.
- Scrapy and Trafilatura are not installed in the local Python environment used during this Codex run.
- `redis-server`, `ffmpeg`, `ffprobe`, and `rhubarb` were not found locally during this Codex run.
- The proof script loads repo `.env*` and `prisma/.env*` files itself via `scripts/load-repo-env.ts`; it does not depend on `dotenv`.
