# Active Open Source Stack

Last audited: 2026-06-18

| Component | Installed | Code wired | Env/service configured | Exact file/route needing fix | Notes |
|---|---|---|---|---|---|
| Redis | Yes, `ioredis` dependency | Yes: `src/lib/redis.ts`, `src/lib/job-queue.ts`, `src/lib/control-plane-jobs.ts` | Requires `REDIS_URL` and running Redis service | `src/app/api/admin/settings/test-redis/route.ts` verifies; no source fix needed unless VPS lacks `REDIS_URL` | Required for durable queue proof. |
| BullMQ worker | Yes, `bullmq` dependency | Yes: `src/lib/worker.ts`, `src/lib/job-queue.ts`, `scripts/worker.mjs` | Requires `REDIS_URL` plus `amarktai-worker.service` or `npm run worker` | `scripts/worker.mjs`, deploy systemd service | Proof remains blocked if the worker is not running with the same deploy. |
| Qdrant/vector store | Yes, `@qdrant/js-client-rest` dependency | Yes: `src/lib/vector-store.ts`, memory/retrieval paths | Requires `QDRANT_URL`; optional `QDRANT_API_KEY` | `src/app/api/admin/settings/integrations/route.ts`, `src/lib/vector-store.ts` | DB fallbacks exist; semantic/vector proof needs Qdrant configured. |
| Playwright | Yes, npm dependency | Yes: `src/lib/local-tools.ts`, research/browser rendering paths | Requires browser install on VPS via `npx playwright install --with-deps chromium` | `src/app/api/admin/settings/runtime-tools/route.ts` only reports it; browser binaries are the usual missing part | Required for rendered research proof. |
| Scrapy | Not an npm dependency; Python module expected | Partially: `src/lib/local-tools.ts`, `src/lib/research-tools.ts`, `scripts/install-open-source-stack.sh` | Requires Python environment with `scrapy` | Research runtime/tooling, not a direct provider adapter | Optional unless research proof requires Python crawl stack. |
| Trafilatura | Not an npm dependency; Python module expected | Partially: `src/lib/local-tools.ts`, `src/lib/research-tools.ts`, `scripts/install-open-source-stack.sh` | Requires Python environment with `trafilatura` | Research extraction path | Optional/required for richer extraction proof. |
| ffmpeg | External binary expected | Yes: `src/lib/local-tools.ts`, `src/lib/long-form-video.ts`, media assembly paths | Requires `ffmpeg` on PATH or `FFMPEG_PATH` | `src/lib/long-form-video.ts` blocks final assembly if missing | Required for video/audio post-processing proof. |
| ffprobe | External binary expected | Yes: `src/lib/local-tools.ts`, `src/lib/long-form-video.ts` | Requires `ffprobe` on PATH or `FFPROBE_PATH` | `src/lib/long-form-video.ts` validates assembled video | Required for long-form video assembly proof. |
| Rhubarb/lip-sync adapter | Missing executable/adapter | Tool check exists in `src/lib/local-tools.ts`; no canonical adapter | Requires binary plus `RHUBARB_PATH` after adapter is implemented | `src/lib/ai-capability-adapters.ts`, `/api/brain/avatar-video` | Current talking-avatar blocker. |
| Local artifact storage | Yes | Yes: `src/lib/storage-root.ts`, `src/lib/storage-driver.ts`, `src/lib/artifact-store.ts` | Requires `AMARKTAI_STORAGE_ROOT`/VPS writable storage | `src/app/api/admin/settings/test-storage/route.ts` verifies | Required for intentional artifacts. |
| Worker service/process | Script exists | Yes: `scripts/worker.mjs`, `src/lib/worker.ts` | Requires a running process manager/systemd service | `docs/deploy/backend-deploy-checklist.md`, `scripts/worker.mjs` | Must be aligned with app deploy before async media proof. |
