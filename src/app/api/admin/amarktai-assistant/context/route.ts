/**
 * GET /api/admin/amarktai-assistant/context
 *
 * Returns live system-state context for AmarktAI Assistant awareness panel.
 * Adapter to the same data as /api/admin/aiva/context.
 */

export { GET } from '@/app/api/admin/aiva/context/route'
